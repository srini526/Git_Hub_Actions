name: multiple jobs

on:
    push:

jobs:

    Build:
        runs-on: ubuntu-latest

        steps:
            - run: Building

    Test:
        runs-on: ubuntu-latest

        steps:
            - run : echo this is Tests
    
    Deploy:
        runs-on: ubuntu-latest

        steps:
            - run: echo this is Deploy

    Cleanup:
        runs-on: ubuntu-latest

        steps:
            - run: echo this is clean UP
