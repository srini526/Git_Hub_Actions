name: Environment variables

on:
    workflow_dispatch:

env:
    NAME: Srinivas
    COURSE: GitHub Actions

jobs:

    demo:
        runs-on: ubuntu-latest

        steps: 

            - run: echo "Hello am $NAME"
            - run: echo "learning $COURSE"