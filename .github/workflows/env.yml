name: Environment variables

on:
    workflow_dispatch:

# env:
#     NAME: Srinivas
#     COURSE: GitHub Actions

jobs:

    build:

        env:
            APP: My App

        runs-on: ubuntu-latest

        steps: 

            - run: echo $APP
            
    Test:

        runs-on: ubuntu-latest

        steps:
            - run: echo $App